import React from "react";
import {Home, LoveStory, Photos, RSVP, Venue} from './';

export const Main = () => (
    <main>
        <Home/>
        <LoveStory/>
        <Photos/>
        <Venue/>
        <RSVP/>
    </main>
);