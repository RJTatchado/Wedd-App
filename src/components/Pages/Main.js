import React from "react";
import {Home, Countdown, LoveStory, Photos, RSVP, Venue} from './';

export const Main = () => (
    <main>
        <Home/>
        <Countdown/>
        <LoveStory/>
        <Photos/>
        <Venue/>
        <RSVP/>
    </main>
);